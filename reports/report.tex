\documentclass[11pt,a4paper]{article}

\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage[margin=2.5cm]{geometry}
\usepackage{graphicx}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{booktabs}
\usepackage{hyperref}

\graphicspath{{figures/}}
\setlength{\emergencystretch}{2em}

\title{Subject 4: Prediction of the Critical Temperature of Superconducting Materials}
\author{\textbf{Authors} Xiaopeng Zhang, Marçal Herraiz Bayó, Shuaibo Huang,\\
Carlos Cosentino, Polina Ptukha, and Lyes Bouchoucha.}
\date{Statistical Learning Project --- \today}

\begin{document}

\maketitle

\section{Introduction and Objective}

Superconducting materials are characterized by a critical temperature above which
their superconducting behavior disappears. Predicting this temperature from
chemical descriptors is a useful statistical learning problem because experimental
measurement is costly, while composition-level information is comparatively easy
to record. In this project, the response variable is
\(Y=\texttt{critical\_temp}\), measured in Kelvin, and the predictors are the 81
numerical physical and chemical covariates supplied in the UCI superconductivity
dataset.

The problem is formulated as supervised regression. Given observations
\((x_i,y_i)\), with \(x_i\in\mathbb{R}^{81}\) and \(y_i\in\mathbb{R}_+\), the
objective is to build a function \(\widehat f\) such that \(\widehat f(x)\)
predicts the critical temperature of a new material as accurately as possible. We
compare a constant baseline, ordinary least squares, Ridge regression, Lasso
regression, k-nearest-neighbor regression, and a histogram-based gradient
boosting regressor. The first four non-baseline methods allow us to connect the
analysis with the course material, while the boosting model is included as an
explicitly explained nonlinear benchmark.

\section{Dataset and Data Audit}

The raw UCI dataset contains 21,263 materials. Each material is described by 81
engineered numerical variables derived from chemical formulae and elemental
properties such as atomic mass, first ionization energy, atomic radius, density,
electron affinity, fusion heat, thermal conductivity, and valence. The engineered
variables include summary statistics such as arithmetic means, weighted means,
geometric means, ranges, entropies, and standard deviations. The target column is
\texttt{critical\_temp}.

Before modeling, exact duplicate rows in the full feature-target table are
removed. This is done before the train-test split, so identical observations
cannot appear once in the training set and once in the test set. The cleaning step
removes 66 exact duplicates and leaves 21,197 observations. No missing values are
present after cleaning, so no imputation policy is needed for this version of the
dataset. The first-stage data audit is saved by the pipeline as JSON and CSV
artifacts under \texttt{reports/tables/}.

The target distribution is right-skewed. The mean critical temperature is
approximately 34.49 K, the median is 20 K, and the maximum is 185 K. This means
that a model can obtain low average error on the many low-temperature materials
while still making scientifically important mistakes in the high-temperature tail.
For that reason, the model comparison is supplemented with residual diagnostics
by target range.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.95\linewidth]{target_distribution.png}
    \caption{Distribution and boxplot of the critical temperature. The response is
    positive and right-skewed, with a long upper tail of high-temperature
    materials.}
\end{figure}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.95\linewidth]{log_target_distribution.png}
    \caption{Distribution and boxplot after the transformation
    \(\log(1+\texttt{critical\_temp})\). The transformed response is less skewed,
    which motivates using this transformation inside the modeling pipelines.}
\end{figure}

\section{Exploratory Data Analysis}

The exploratory analysis has three goals: to understand the target variable, to
check the quality and structure of the covariates, and to identify possible
nonlinear relationships that could influence model choice. The marginal Pearson
correlations show that several thermal conductivity, atomic radius, valence,
atomic mass, and ionization-energy descriptors are strongly related to the target.
These correlations are not used as a feature selection rule; they are descriptive
statistics that help interpret the dataset and the behavior of later models.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.9\linewidth]{feature_target_correlations.png}
    \caption{The twenty features with the largest absolute Pearson correlation
    with the critical temperature. The signs indicate whether the marginal linear
    relation is positive or negative.}
\end{figure}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.9\linewidth]{selected_feature_correlation_matrix.png}
    \caption{Correlation matrix for the features most correlated with the target.
    Strong dependence between covariates is visible, which supports the use of
    regularized linear models such as Ridge and Lasso.}
\end{figure}

The feature families have different levels of marginal association with the
response. The strongest single correlation is obtained by
\texttt{wtd\_std\_ThermalConductivity}; thermal conductivity, atomic radius,
valence, atomic mass, and the number of elements all contain variables with
absolute correlations above 0.60. These observations are consistent with the
physical intuition that both composition complexity and elemental property
variation matter.

\begin{table}[htbp]
    \centering
    \resizebox{\linewidth}{!}{%
    \IfFileExists{reports/tables/feature_family_correlation_summary.tex}
      {\input{reports/tables/feature_family_correlation_summary.tex}}
      {\input{tables/feature_family_correlation_summary.tex}}
    }
    \caption{Correlation summary by engineered feature family. For each family,
    the table reports the average absolute marginal correlation and the strongest
    individual feature.}
\end{table}

The number of chemical elements in the formula is itself an informative covariate.
Single-, two-, and three-element materials are mostly low-temperature in this
dataset, whereas materials with four or more elements are much more likely to
reach high critical temperatures. The 77 K threshold is useful as a descriptive
reference because it corresponds approximately to the boiling point of liquid
nitrogen and is often used to distinguish practically attractive high-temperature
superconductors.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.95\linewidth]{target_by_number_of_elements.png}
    \caption{Critical temperature as a function of the number of elements. The
    boxplots show the shift in the target distribution, and the bar plot shows the
    proportion of materials with critical temperature at least 77 K.}
\end{figure}

\begin{table}[htbp]
    \centering
    \resizebox{\linewidth}{!}{%
    \IfFileExists{reports/tables/target_by_number_of_elements.tex}
      {\input{reports/tables/target_by_number_of_elements.tex}}
      {\input{tables/target_by_number_of_elements.tex}}
    }
    \caption{Target summary by number of elements in the material formula. The
    share column reports the fraction of observations with critical temperature
    at least 77 K.}
\end{table}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.85\linewidth]{feature_family_correlations.png}
    \caption{Largest absolute marginal target correlation within each engineered
    physical-property family. This complements the individual feature ranking by
    grouping related descriptors.}
\end{figure}

\section{Preprocessing and Leakage Control}

The preprocessing sequence is designed to make the final test-set results
credible. First, the raw UCI feature and target tables are merged and exact full
rows are removed. Second, the cleaned table is split once into a training set and
a held-out test set using an 80/20 split with random seed 42. The held-out test set
is not used for hyperparameter selection.

For OLS, Ridge, Lasso, and k-nearest neighbors, the covariates are standardized
inside a scikit-learn pipeline. The centering and scaling parameters are fit only
on the relevant training fold during cross-validation and only on the full
training set before final test evaluation. This avoids data leakage from the test
set into preprocessing. The tree-based histogram gradient boosting model is not
sensitive to monotone feature scaling, so it is fit without standardization.

The target is transformed during fitting using
\[
Z=\log(1+Y).
\]
Predictions are transformed back to the original Kelvin scale using
\(\widehat Y=\exp(\widehat Z)-1\) before metrics are computed. The transformation
reduces the dominance of the high-temperature tail during fitting and ensures that
model comparisons are still reported in physically interpretable Kelvin units.

\section{Models}

Let \(z_i=\log(1+y_i)\) denote the transformed target used during fitting. For
models with standardization, let
\[
\widetilde x_i = D^{-1}(x_i-\mu),
\]
where \(\mu\) and \(D\) are the training-set feature means and standard
deviations. All formulas below describe predictions on the transformed scale; the
reported Kelvin prediction is always
\[
\widehat y(x)=\exp(\widehat z(x))-1.
\]

\paragraph{Constant baseline.}
The baseline predicts a constant value learned from the training data. It provides
a minimal reference: a useful model must improve substantially on this baseline.
With \(n_{\mathrm{train}}\) training observations, the fitted constant is
\[
\widehat z(x)=\widehat c,\qquad
\widehat c=\frac{1}{n_{\mathrm{train}}}\sum_{i=1}^{n_{\mathrm{train}}}z_i.
\]
Thus the baseline ignores all covariates and predicts the training mean on the
log-transformed scale.

\paragraph{Ordinary least squares.}
The ordinary least squares model is the affine function
\[
\widehat z(x)=\widehat\beta_0+\widetilde x^\top\widehat\beta,
\]
where
\[
(\widehat\beta_0,\widehat\beta)
=\arg\min_{\beta_0,\beta}
\sum_{i=1}^{n_{\mathrm{train}}}
\left(z_i-\beta_0-\widetilde x_i^\top\beta\right)^2 .
\]
OLS is simple and interpretable, but it can be unstable when predictors are
correlated.

\paragraph{Ridge regression.}
Ridge regression adds an \(\ell_2\) penalty to stabilize the linear model:
\[
(\widehat\beta_0,\widehat\beta)
=\arg\min_{\beta_0,\beta}
\sum_{i=1}^{n_{\mathrm{train}}}
\left(z_i-\beta_0-\widetilde x_i^\top\beta\right)^2+
\lambda\sum_{j=1}^{p}\beta_j^2,
\qquad
\widehat z(x)=\widehat\beta_0+\widetilde x^\top\widehat\beta .
\]
The penalty shrinks coefficients toward zero without setting them exactly to zero.
The regularization parameter is selected by cross-validation on the training set.

\paragraph{Lasso regression.}
Lasso regression replaces the Ridge penalty by an \(\ell_1\) penalty:
\[
(\widehat\beta_0,\widehat\beta)
=\arg\min_{\beta_0,\beta}
\sum_{i=1}^{n_{\mathrm{train}}}
\left(z_i-\beta_0-\widetilde x_i^\top\beta\right)^2+
\lambda\sum_{j=1}^{p}|\beta_j|,
\qquad
\widehat z(x)=\widehat\beta_0+\widetilde x^\top\widehat\beta .
\]
This can set some coefficients exactly to zero, which makes the fitted linear
model more sparse. As with Ridge, the penalty parameter is selected without using
the held-out test set.

\paragraph{k-nearest-neighbor regression.}
k-NN predicts a new response by averaging the responses of the closest training
observations in standardized feature space. For a new point \(x\), define
\[
\mathcal N_k(\widetilde x)=
\left\{i:\widetilde x_i \text{ is among the } k \text{ smallest values of }
\|\widetilde x-\widetilde x_i\|_2\right\}.
\]
The uniform-weight k-NN prediction is
\[
\widehat z(x)=\frac{1}{k}\sum_{i\in\mathcal N_k(\widetilde x)} z_i .
\]
It is nonlinear and can adapt to local structure in the data. The number of
neighbors \(k\) is selected by grid search with cross-validation. This method is
powerful here but stores the training set and can be sensitive to the choice of
scaling and distance metric.

\paragraph{Histogram-based gradient boosting.}
The histogram gradient boosting regressor is included as an additional nonlinear
benchmark. It builds an additive model of regression trees on the transformed
target. With squared-error loss, the model has the form
\[
\widehat z(x)=F_M(x),\qquad
F_M(x)=F_0(x)+\nu\sum_{m=1}^{M} h_m(x),
\]
where \(F_0(x)=\bar z_{\mathrm{train}}\), \(\nu\) is the learning rate, and each
tree is a piecewise constant function
\[
h_m(x)=\sum_{\ell=1}^{L_m}\gamma_{m\ell}\mathbf 1\{x\in R_{m\ell}\}.
\]
At boosting step \(m\), the current residuals are
\[
r_{im}=z_i-F_{m-1}(x_i),
\]
and the tree partitions \(R_{m1},\ldots,R_{mL_m}\) are chosen from histogram-binned
feature splits to reduce squared error. For a fixed leaf \(R_{m\ell}\), the leaf
value is equivalently
\[
\gamma_{m\ell}
=\arg\min_{\gamma}
\sum_{i:x_i\in R_{m\ell}}\left(r_{im}-\gamma\right)^2+
\lambda_2\gamma^2,
\]
where \(\lambda_2\) is the tree leaf \(\ell_2\) regularization parameter. This
method is not treated as a course-covered method here, so its functioning is
stated explicitly. It is useful for this dataset because interactions between
chemical descriptors are plausible and may not be captured by linear models.

\section{Model Selection and Evaluation Criteria}

Model selection is performed only with the training data. Ridge and Lasso choose
regularization strengths from predefined grids, and k-NN chooses
\(k\in\{3,5,7,11,15,21,31\}\) by five-fold cross-validation. The gradient boosting
hyperparameters are fixed in the configuration file and include a learning rate of
0.06, 350 maximum boosting iterations, 31 maximum leaf nodes, \(\ell_2\)
regularization 0.01, and early stopping on an internal validation fraction.

For observations \((y_i,\widehat y_i)_{i=1}^n\) in the held-out test set, the main
criteria are
\[
\mathrm{MSE}=\frac{1}{n}\sum_{i=1}^n (y_i-\widehat y_i)^2,\qquad
\mathrm{RMSE}=\sqrt{\mathrm{MSE}},
\]
\[
\mathrm{MAE}=\frac{1}{n}\sum_{i=1}^n |y_i-\widehat y_i|,\qquad
R^2=1-\frac{\sum_{i=1}^n (y_i-\widehat y_i)^2}
{\sum_{i=1}^n (y_i-\bar y)^2}.
\]
RMSE is the primary selection criterion because it is expressed in Kelvin and
penalizes large errors strongly. MAE is less sensitive to isolated large errors,
and \(R^2\) measures the fraction of test-set variance explained relative to a
constant predictor.

\section{Results}

\begin{table}[htbp]
    \centering
    \IfFileExists{reports/tables/final_model_comparison.tex}
      {\input{reports/tables/final_model_comparison.tex}}
      {\input{tables/final_model_comparison.tex}}
    \caption{Held-out test performance. Hyperparameters are selected on the
    training data, and this table is computed only on the test set.}
\end{table}

The nonlinear models outperform the linear family by a large margin. The
k-nearest-neighbor model obtains the lowest RMSE, approximately 10.72 K, the
lowest MAE, approximately 5.72 K, and the highest \(R^2\), approximately 0.902.
The histogram-based gradient boosting model is very close, with RMSE approximately
10.75 K and \(R^2\) approximately 0.902. OLS, Ridge, and Lasso remain around RMSE
18.3 K. This gap suggests that the relationship between the engineered physical
descriptors and the critical temperature contains nonlinear structure or
interactions that linear models do not capture well.

The best selected k-NN model uses \(k=3\), while Ridge selects a small penalty
\(\alpha=0.1\) and Lasso selects \(\alpha=10^{-4}\). The small amount of
regularization selected for the linear models is consistent with the fact that the
linear signal is present but insufficient: shrinkage stabilizes coefficients, yet
it cannot create the nonlinear local behavior captured by k-NN and boosting.

\begin{table}[htbp]
    \centering
    \resizebox{\linewidth}{!}{%
    \IfFileExists{reports/tables/final_model_residuals_summary.tex}
      {\input{reports/tables/final_model_residuals_summary.tex}}
      {\input{tables/final_model_residuals_summary.tex}}
    }
    \caption{Residual diagnostics by model on the held-out test set. Residuals are
    defined as observed minus predicted critical temperature in Kelvin.}
\end{table}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.68\linewidth]{best_model_predicted_vs_observed.png}
    \caption{Predicted versus observed critical temperature for the selected
    model. Points near the diagonal correspond to accurate predictions on the
    Kelvin scale.}
\end{figure}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.82\linewidth]{best_model_residual_diagnostics.png}
    \caption{Residuals of the selected model as a function of predicted critical
    temperature. This diagnostic checks whether large errors are concentrated in
    specific prediction ranges.}
\end{figure}

The residual diagnostics indicate that the selected model has little average bias
overall, with a test residual mean of about 0.41 K. However, the error is not
uniform over the target range. Predictions are most accurate for low-temperature
materials below 10 K. Errors are larger for the high-temperature tail, especially
for materials above 100 K, where the model tends to underpredict on average. This
is an important limitation because the high-temperature region is often the most
scientifically interesting part of the dataset.

\begin{table}[htbp]
    \centering
    \resizebox{\linewidth}{!}{%
    \IfFileExists{reports/tables/best_model_error_by_target_range.tex}
      {\input{reports/tables/best_model_error_by_target_range.tex}}
      {\input{tables/best_model_error_by_target_range.tex}}
    }
    \caption{Selected-model error by observed critical-temperature range. The
    table highlights that test error increases in the high-temperature tail.}
\end{table}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.78\linewidth]{best_model_error_by_target_range.png}
    \caption{MAE and RMSE of the selected model by observed target range. The
    highest-temperature materials are the most difficult for the model.}
\end{figure}

\section{Discussion}

The comparison leads to two main conclusions. First, the nonlinear structure in
the dataset is strong: k-NN and gradient boosting improve the RMSE by more than
7 K compared with the linear methods. Second, the best predictive model is not
necessarily the most interpretable model. Ridge and Lasso summarize the marginal
linear contribution of standardized descriptors and are easier to explain, but
both lose substantial predictive accuracy. k-NN gives the best held-out metrics in
this run, while gradient boosting is nearly as accurate and may be easier to use
in production because prediction does not require searching through all training
observations.

The target transformation also matters. Fitting on \(\log(1+Y)\) reduces the
influence of extremely high target values and helps all methods produce positive,
stable predictions after inverse transformation. The trade-off is that the fitted
objective is not exactly the same as minimizing squared error directly in Kelvin.
This trade-off is acceptable here because final evaluation is always performed on
the original scale and because the transformed target is much less skewed.

\section{Reproducibility}

The results can be reproduced by executing
\path{notebooks/final_submission.ipynb}. The notebook calls reusable Python
modules under \path{src/}, which implement the data audit, preprocessing, model
fitting, and evaluation steps.

The configuration file \path{configs/default.yaml} records the random seed, the
80/20 train-test split, the five-fold cross-validation setting, the metric list,
and the model hyperparameter grids. Package versions are managed with
\texttt{uv} through \path{pyproject.toml} and \path{uv.lock}. To reproduce the
analysis in the project repository, one first runs \texttt{uv sync} and then
executes \path{notebooks/final_submission.ipynb} with the synced Python
environment. The random seed is set before model training, preprocessing is fit
inside each training fold or training split, and the held-out test set is used
only for final reporting.

\section{Limitations and Possible Extensions}

The final model is evaluated on a random held-out split, not on an external
experimental dataset. The reported error therefore measures generalization to
similar observations from the same data source, not necessarily to future
materials synthesized under different experimental conditions. In addition, the
analysis uses the engineered UCI descriptors directly and does not reconstruct
chemical features from raw formulae. Some materials with very high critical
temperature are still underpredicted, suggesting that more specialized features,
local models for the high-temperature tail, or uncertainty estimates could be
useful extensions.

Another limitation is interpretability. The strongest model, k-NN, is local and
non-parametric. It explains a prediction through nearby training examples rather
than through global coefficients. For a scientific report, Ridge and Lasso remain
valuable because they provide a simpler linear reference, but their predictive
performance is clearly lower. Future work could add model-agnostic interpretation
tools or compare with additional methods after explaining them carefully, provided
that the course-method distinction is maintained.

\section{Conclusion}

The project defines a supervised regression problem for predicting
\texttt{critical\_temp} from 81 physical and chemical covariates. After duplicate
removal, leakage-controlled preprocessing, cross-validation on the training set,
and final evaluation on a held-out test set, k-nearest-neighbor regression with
\(k=3\) is the recommended model. It achieves RMSE about 10.72 K, MAE about
5.72 K, and \(R^2\) about 0.902. Histogram-based gradient boosting is a very close
nonlinear alternative. Linear regression, Ridge, and Lasso are useful baselines, but they are not accurate enough to match the
nonlinear methods on this dataset. The final diagnostic analysis shows that the
main remaining challenge is accurate prediction for the high-temperature tail.

\end{document}
