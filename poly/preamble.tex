% ==========================================================================
% Uniform preamble for: Machine Learning Algorithms — from Classical Methods
%   to Deep Neural Networks (Gabriel Turinici)
% Used by: main.tex and every chap*.tex file in this folder.
% ==========================================================================

% --- Encoding, fonts, microtypography -------------------------------------
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage{microtype}

% --- Page layout ----------------------------------------------------------
\usepackage[a4paper,margin=1in]{geometry}
\usepackage{setspace}

% --- Math -----------------------------------------------------------------
\usepackage{amsmath, amssymb, amsthm, mathtools}
\usepackage{bm}
\usepackage{dsfont}   % \mathds{1} for indicators if desired

% --- Graphics, colour, code, algorithms -----------------------------------
\usepackage{graphicx}
\usepackage[dvipsnames]{xcolor}
\usepackage{tikz}
\usetikzlibrary{arrows.meta, positioning, fit, calc}
\usepackage{algorithm}
\usepackage{algpseudocode}
\usepackage{listings}
\lstset{
  basicstyle=\ttfamily\small,
  keywordstyle=\color{blue!60!black}\bfseries,
  commentstyle=\color{gray},
  stringstyle=\color{red!50!black},
  showstringspaces=false,
  breaklines=true,
  frame=single,
  rulecolor=\color{black!30},
}

% --- Tables and lists -----------------------------------------------------
\usepackage{booktabs, array, longtable, multirow}
\usepackage{enumitem}
\setlist[itemize]{topsep=3pt, itemsep=3pt, parsep=0pt, leftmargin=1.5em}
\setlist[enumerate]{topsep=3pt, itemsep=3pt, parsep=0pt, leftmargin=1.8em}

% --- Hyperlinks, cross references, captions -------------------------------
\usepackage{caption}
\usepackage{subcaption}
\usepackage{hyperref}
\hypersetup{
  colorlinks=true,
  linkcolor=blue!50!black,
  urlcolor=blue!50!black,
  citecolor=blue!50!black,
  pdftitle={Machine Learning Algorithms: From Classical Methods to Deep Neural Networks},
  pdfauthor={Someone who doesn't like to share}
}
\usepackage{cleveref}

% ==========================================================================
% Theorem-like environments
% ==========================================================================
\theoremstyle{definition}
\newtheorem{definition}{Definition}[chapter]
\newtheorem{example}[definition]{Example}

\theoremstyle{plain}
\newtheorem{theorem}[definition]{Theorem}
\newtheorem{proposition}[definition]{Proposition}
\newtheorem{lemma}[definition]{Lemma}
\newtheorem{corollary}[definition]{Corollary}

\theoremstyle{remark}
\newtheorem{remark}[definition]{Remark}
\newtheorem{warning}[definition]{Warning}
\newtheorem*{note}{Note}

% Advanced topic box (used in the source as "Advanced topics X.Y")
\newtheorem{advancedtopics}[definition]{Advanced topics}

% ==========================================================================
% Shorthand macros — keep consistent across all chapters!
% ==========================================================================

% Number sets
\newcommand{\R}{\mathbb{R}}
\newcommand{\N}{\mathbb{N}}
\newcommand{\Z}{\mathbb{Z}}
\newcommand{\Q}{\mathbb{Q}}
\newcommand{\C}{\mathbb{C}}

% Probability objects
\newcommand{\E}{\mathbb{E}}
\newcommand{\PP}{\mathbb{P}}
\newcommand{\Var}{\mathrm{Var}}
\newcommand{\Cov}{\mathrm{Cov}}
\newcommand{\ind}{\mathds{1}}          % indicator
\newcommand{\bone}{\mathbf{1}}         % bold one (vector of ones)

% Calligraphic / script symbols frequently used
\newcommand{\X}{\mathbb{X}}            % design matrix (Ch. 4)
\newcommand{\Y}{\mathbb{Y}}            % response vector
\newcommand{\cL}{\mathcal{L}}          % loss
\newcommand{\cN}{\mathcal{N}}          % normal distribution / network
\newcommand{\cD}{\mathcal{D}}          % dataset
\newcommand{\cX}{\mathcal{X}}          % feature space
\newcommand{\cY}{\mathcal{Y}}          % label space
\newcommand{\cA}{\mathcal{A}}          % activation / algorithm
\newcommand{\cC}{\mathcal{C}}          % classes / clusters
\newcommand{\cR}{\mathcal{R}}          % risk / empirical risk
\newcommand{\cG}{\mathcal{G}}          % function class
\newcommand{\cF}{\mathcal{F}}          % function family
\newcommand{\cH}{\mathcal{H}}          % hypothesis space
\newcommand{\cB}{\mathcal{B}}          % ball / batch
\newcommand{\cM}{\mathcal{M}}          % manifold / model
\newcommand{\cP}{\mathcal{P}}          % probability measure / partition
\newcommand{\cT}{\mathcal{T}}          % training / test
\newcommand{\cI}{\mathcal{I}}          % index set
\newcommand{\cS}{\mathcal{S}}          % sample / set
\newcommand{\cO}{\mathcal{O}}          % big-O
\newcommand{\cE}{\mathcal{E}}          % event sigma-algebra / exponential family

% Distributions
\newcommand{\Normal}{\mathcal{N}}
\newcommand{\Unif}{\mathcal{U}}
\DeclareMathOperator{\Ber}{Bernoulli}
\DeclareMathOperator{\Bino}{Binomial}
\DeclareMathOperator{\Pois}{Poisson}

% Operators
\DeclareMathOperator*{\argmin}{arg\,min}
\DeclareMathOperator*{\argmax}{arg\,max}
\DeclareMathOperator{\sign}{sign}
\DeclareMathOperator{\rank}{rank}
\DeclareMathOperator{\tr}{tr}
\DeclareMathOperator{\diag}{diag}
\DeclareMathOperator{\softmax}{softmax}
\DeclareMathOperator{\ReLU}{ReLU}

% Relations
\newcommand{\iid}{\stackrel{\text{i.i.d.}}{\sim}}
\newcommand{\deq}{\stackrel{d}{=}}
\newcommand{\indep}{\mathrel{\perp\!\!\!\perp}}

% Differentials and small helpers
\newcommand{\dd}{\,\mathrm{d}}
\newcommand{\grad}{\nabla}
\newcommand{\eps}{\varepsilon}
\newcommand{\T}{^{\top}}               % transpose

% Paired delimiters: \abs*{...}, \norm*{...}, \inner*{..}{..}
\DeclarePairedDelimiter{\abs}{\lvert}{\rvert}
\DeclarePairedDelimiter{\norm}{\lVert}{\rVert}
\DeclarePairedDelimiterX{\inner}[2]{\langle}{\rangle}{#1,\,#2}

% Identity matrix and zero vector
\newcommand{\Id}{\mathrm{Id}}
\newcommand{\Zero}{\mathbf{0}}

% ==========================================================================
% Highlighting boxes (the source PDF uses coloured warning / advanced boxes)
% ==========================================================================
\usepackage[most]{tcolorbox}
\tcbset{
  warningbox/.style={
    colback=red!5!white, colframe=red!60!black,
    boxrule=0.4pt, arc=1pt, left=6pt, right=6pt, top=3pt, bottom=3pt,
  },
  advancedbox/.style={
    colback=blue!3!white, colframe=blue!40!black,
    boxrule=0.4pt, arc=1pt, left=6pt, right=6pt, top=3pt, bottom=3pt,
  },
  remarkbox/.style={
    colback=gray!5!white, colframe=gray!60!black,
    boxrule=0.3pt, arc=1pt, left=6pt, right=6pt, top=3pt, bottom=3pt,
  }
}
